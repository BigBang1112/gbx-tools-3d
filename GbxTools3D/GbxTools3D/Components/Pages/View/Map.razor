@page "/view/map"
@inject HttpClient Http

<GbxTools3D.Client.Pages.Counter TmxId="@tmxId"></GbxTools3D.Client.Pages.Counter>

@code {
    private string tmxId;

    protected override async Task OnInitializedAsync()
    {
        //await Task.Delay(10000);

        //using var response = await Http.GetAsync("https://tmnf.exchange/trackgbx/7218082");

        //tmxId = Convert.ToBase64String(await response.Content.ReadAsByteArrayAsync());
    }
}
